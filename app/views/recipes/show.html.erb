<div class="row">

  <div class="col-md-4">

	<%= image_tag @recipe.recipe_img.url(:recipe_show), class: "recipe-show" %>

  </div> <!-- col-md-4 -->
  
  <div class="col-md-8">

	<h2><%= @recipe.title %></h2>
	<h4>Posted by: <%= @recipe.user.username.capitalize %></h4>
	<p><%= @recipe.description %></p>

  </div> <!-- col-md-8 -->

</div> <!-- row -->

  <div class="row">
  	
  	<div class="col-md-4">

  		<!-- Reviews / Rating will go here -->
  		<!-- TODO FIX paperclip sizing -->


  	</div>

  </div>

<%= link_to "Back", root_path %>
<% if user_signed_in? %> 
	<% if @recipe.user_id == current_user.id || current_user.admin %>
	  <%= link_to "Edit", edit_recipe_path(@recipe) %>
	  <%= link_to "Delete", recipe_path(@recipe), method: :delete, data: { confirm: "Are you sure?" } %>
	<% end %>
<% end %>